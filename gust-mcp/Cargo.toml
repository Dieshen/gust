[package]
name = "gust-mcp"
version = "0.2.0"
edition = "2021"

[dependencies]
gust-lang = { path = "../gust-lang" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
