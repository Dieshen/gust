[package]
name = "gust-runtime"
version = "0.2.0"
edition = "2021"
description = "Runtime support library for compiled Gust programs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tokio = { version = "1", features = ["full"] }
