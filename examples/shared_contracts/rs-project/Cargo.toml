[package]
name = "shared-contracts-rs-project"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
gust-runtime = { path = "../../../gust-runtime" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
tokio = { version = "1", features = ["full"] }
